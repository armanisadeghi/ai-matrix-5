/*
import { createTable, getTableData, updateTableData, deleteTableData } from './crud';

// Example of creating a new movie
const newMovie = await createTable('movies', {
    name: 'Inception',
    data: null
});

// Example of fetching movies
const movies = await getTableData('movies');

// Example of updating a movie
await updateTableData('movies', newMovie.id, {
    name: 'Inception Updated'
});

// Example of deleting a movie
await deleteTableData('movies', newMovie.id);
*/
