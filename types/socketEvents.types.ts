/*
import { SocketTaskObject } from '@/utils/socketio/SocketManager';


export interface BrokerValue {
    id: string;
    official_name: string;
    value: any;
    data_type: string;
    ready_status: boolean;
}

export interface Overrides {
    model_override: Record<string, any>;
    processor_override: Record<string, any>;
    other_overrides: Record<string, any>;
}

export interface SimpleRecipeData {
    recipe_id: string;
    broker_values: BrokerValue[];
    overrides: Overrides;
    stream: boolean;
}

// Now, extend the SocketTaskObject for a 'simple_recipe' task
export interface SimpleRecipeTask extends SocketTaskObject {
    taskData: SimpleRecipeData;
}

// Finally, define the type for SocketTask when it's specifically a list of 'simple_recipe' tasks
*/
